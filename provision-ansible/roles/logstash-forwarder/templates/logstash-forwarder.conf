{
  "network": {
    "servers": [ "{{ logstash_server }}" ],
    "timeout": {{ logstash_timeout }},
    #"ssl ca": "{{ origin_cert }}"
    "ssl certificate": "{{ origin_cert }}"
  },

  "files": [
    {
    {{ files_to_log }}
    }
  ]
}

